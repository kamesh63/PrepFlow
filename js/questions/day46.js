window.QUIZ_DATA = window.QUIZ_DATA || {};
window.QUIZ_DATA["day46"] = {
  title: "DataStage Case Study",
  topics: ["ETL Design Patterns", "Error Handling", "Performance Tuning", "Job Scheduling", "End-to-End Project"],
  questions: [
    {
      id: 1,
      type: "single",
      difficulty: 1,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 2,
      type: "single",
      difficulty: 1,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 3,
      type: "single",
      difficulty: 1,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 4,
      type: "single",
      difficulty: 1,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 5,
      type: "single",
      difficulty: 1,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 6,
      type: "single",
      difficulty: 1,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 7,
      type: "single",
      difficulty: 1,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 8,
      type: "single",
      difficulty: 1,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 9,
      type: "single",
      difficulty: 1,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 10,
      type: "single",
      difficulty: 1,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 11,
      type: "single",
      difficulty: 1,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 12,
      type: "single",
      difficulty: 1,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 13,
      type: "single",
      difficulty: 1,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 14,
      type: "single",
      difficulty: 1,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 15,
      type: "single",
      difficulty: 1,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 16,
      type: "single",
      difficulty: 1,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 17,
      type: "single",
      difficulty: 1,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 18,
      type: "single",
      difficulty: 1,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 19,
      type: "single",
      difficulty: 1,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 20,
      type: "single",
      difficulty: 1,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 21,
      type: "single",
      difficulty: 1,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 22,
      type: "single",
      difficulty: 1,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 23,
      type: "single",
      difficulty: 1,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 24,
      type: "single",
      difficulty: 1,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 25,
      type: "single",
      difficulty: 1,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 26,
      type: "single",
      difficulty: 1,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 27,
      type: "single",
      difficulty: 1,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 28,
      type: "single",
      difficulty: 1,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 29,
      type: "single",
      difficulty: 1,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 30,
      type: "single",
      difficulty: 1,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 31,
      type: "single",
      difficulty: 1,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 32,
      type: "single",
      difficulty: 1,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 33,
      type: "single",
      difficulty: 1,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 34,
      type: "single",
      difficulty: 1,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 35,
      type: "single",
      difficulty: 1,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 36,
      type: "single",
      difficulty: 2,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 37,
      type: "single",
      difficulty: 2,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 38,
      type: "single",
      difficulty: 2,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 39,
      type: "single",
      difficulty: 2,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 40,
      type: "single",
      difficulty: 2,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 41,
      type: "single",
      difficulty: 2,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 42,
      type: "single",
      difficulty: 2,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 43,
      type: "single",
      difficulty: 2,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 44,
      type: "single",
      difficulty: 2,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 45,
      type: "single",
      difficulty: 2,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 46,
      type: "single",
      difficulty: 2,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 47,
      type: "single",
      difficulty: 2,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 48,
      type: "single",
      difficulty: 2,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 49,
      type: "single",
      difficulty: 2,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 50,
      type: "single",
      difficulty: 2,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 51,
      type: "single",
      difficulty: 2,
      question: "During `parsing` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 52,
      type: "single",
      difficulty: 2,
      question: "During `optimizing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 53,
      type: "single",
      difficulty: 2,
      question: "During `buffering` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 54,
      type: "single",
      difficulty: 2,
      question: "During `checkpointing` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 55,
      type: "single",
      difficulty: 2,
      question: "During `materializing` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 56,
      type: "single",
      difficulty: 2,
      question: "During `partitioning` on `ETL Design Patterns` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 57,
      type: "single",
      difficulty: 2,
      question: "During `indexing` on `Error Handling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 58,
      type: "single",
      difficulty: 2,
      question: "During `governing` on `Performance Tuning` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 59,
      type: "single",
      difficulty: 2,
      question: "During `validating` on `Job Scheduling` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 60,
      type: "single",
      difficulty: 2,
      question: "During `transforming` on `End-to-End Project` inside a DataStage Parallel Job, you notice that the transformer stage is causing a performance bottleneck. The input partition type is set to Auto. What is the most effective tuning adjustment?",
      options: ["Manually configure partitioning keys on the input link to match processing needs   ", "Increase the system configuration engine scratch space size parameters             ", "Insert a Sequential File Stage immediately before the Transformer container stage  ", "Disable compilation optimizations within the Administrator runtime control panel   "],
      correct: [0],
      concept: "In IBM DataStage parallel jobs, setting partition types manually (e.g. Hash partitioning by a key) prevents unnecessary repartitioning overhead during execution, which occurs when using \'Auto\' across adjacent stages."
    },
    {
      id: 61,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `parsing` on `ETL Design Patterns`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 62,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `optimizing` on `Error Handling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 63,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `buffering` on `Performance Tuning`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 64,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `checkpointing` on `Job Scheduling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 65,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `materializing` on `End-to-End Project`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 66,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `partitioning` on `ETL Design Patterns`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 67,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `indexing` on `Error Handling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 68,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `governing` on `Performance Tuning`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 69,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `validating` on `Job Scheduling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 70,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `transforming` on `End-to-End Project`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 71,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `parsing` on `ETL Design Patterns`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 72,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `optimizing` on `Error Handling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 73,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `buffering` on `Performance Tuning`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 74,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `checkpointing` on `Job Scheduling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 75,
      type: "multi",
      difficulty: 2,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `materializing` on `End-to-End Project`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 76,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `partitioning` on `ETL Design Patterns`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 77,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `indexing` on `Error Handling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 78,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `governing` on `Performance Tuning`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 79,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `validating` on `Job Scheduling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 80,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `transforming` on `End-to-End Project`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 81,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `parsing` on `ETL Design Patterns`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 82,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `optimizing` on `Error Handling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 83,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `buffering` on `Performance Tuning`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 84,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `checkpointing` on `Job Scheduling`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 85,
      type: "multi",
      difficulty: 3,
      question: "You are constructing a complex parallel job in DataStage Designer to perform `materializing` on `End-to-End Project`. Which of the following represent core Parallel execution stages? (Select ALL that apply)",
      options: ["A: Transformer Stage to execute key mappings, variable allocations, and functions   ", "B: Join Stage to perform inner, left, or right joins across multiple data streams   ", "C: Sequential File Stage to read flat delimiter files from a file storage system    ", "D: Parameter Set Stage to build runtime parameters across administrator panels      "],
      correct: [0, 1, 2],
      concept: "Transformer, Join, and Sequential File are standard operational stages inside IBM DataStage Parallel Jobs. Parameter Sets are administrative objects, not execution stages."
    },
    {
      id: 86,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 87,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 88,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 89,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 90,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 91,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 92,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 93,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 94,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 95,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 96,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 97,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 98,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 99,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    },
    {
      id: 100,
      type: "tf",
      difficulty: 3,
      question: "Is it true that in DataStage Parallel Jobs, all data processing is executed on a single engine thread process, completely disabling pipeline parallelism?",
      options: ["True, the parallel engine uses single-threaded joins", "False, DataStage splits rows across multiple nodes  "],
      correct: [1],
      concept: "False. DataStage parallel jobs use a configuration file (.apt) to spawn multiple execution processes (nodes) across servers, achieving both pipeline and partition parallelism."
    }
  ]
};
